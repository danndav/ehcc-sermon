import { Injectable } from '@nestjs/common';

@Injectable()
export class PaystackService {
  // Paystack integration: initializeTransaction, verifyTransaction, createSubscription, cancelSubscription
}
